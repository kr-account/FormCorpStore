Runs on PHP 7, Laravel 5.2 on a Homestead VM running MySQL

1. Point the webroot to FormCorpStore/public
2. To create the Pets, run FormCorpStore/resources/pets.sql
3. Change the DB.* items in FormCorpStore/.env to reflect database settings.

 
